// Пример конфигурации для Voice PC Cloud Function
// Скопируйте этот файл в config.js и настройте под свои нужды

const config = {
    // ОБЯЗАТЕЛЬНО: URL вашего домена
    // Замените на свой домен или Cloudflare Tunnel URL
    serverUrl: 'https://your-domain.com',
    
    // API эндпоинт для отправки команд
    apiEndpoint: '/command',
    
    // Токен аутентификации для доступа к локальному серверу
    // ВАЖНО: Должен совпадать с ALICE_TOKEN на локальном сервере
    // Используйте случайную строку длиной не менее 32 символов
    authToken: 'VoicePC_SecureToken_2024_CHANGE_ME_TO_RANDOM_STRING',
    
    // Таймаут запросов в миллисекундах
    timeout: 5000,
    
    // Количество попыток при неудаче
    retryAttempts: 2,
    
    // Задержка между попытками (мс)
    retryDelay: 1000,
    
    // Уровень логирования: 'debug', 'info', 'warn', 'error'
    logLevel: 'info',
    
    // Фразы для ответов Алисы
    responses: {
        welcome: [
            'Привет! Готова выполнять команды.',
            'Здравствуйте! Я готова управлять вашим компьютером.',
            'Привет! Что будем делать с компьютером?'
        ],
        
        success: [
            'Готово!',
            'Выполнено!',
            'Сделала!',
            'Команда выполнена!'
        ],
        
        error: [
            'Произошла ошибка при выполнении команды.',
            'Не удалось выполнить команду.',
            'Что-то пошло не так.'
        ],
        
        unknownCommand: [
            'Не понимаю эту команду. Попробуйте другую.',
            'Такой команды я не знаю.',
            'Команда не распознана. Скажите что-нибудь другое.'
        ]
    },
    
    // Настройки безопасности
    security: {
        // Разрешенные IP адреса (пустой массив = разрешить все)
        allowedIPs: [],
        
        // Максимальное количество запросов в минуту
        rateLimit: 60,
        
        // Блокировать подозрительные команды
        blockSuspiciousCommands: true
    }
};

module.exports = config;